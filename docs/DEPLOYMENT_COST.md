# Deployment cost

No cloud-cost estimate is claimed for this prototype. Size a deployment only after measuring target traffic, retention, HA, and security-boundary requirements. The local Compose API is capped at 0.5 CPU and 256 MiB as a reproducible demo envelope, not a production sizing recommendation.
